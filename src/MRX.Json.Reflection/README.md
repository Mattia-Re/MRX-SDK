# MRX.Json.Reflection

Reflection-based property accessors for JSON model nodes, built on top of `MRX.Json` and
`MRX.Parsing.Reflection`.

```
dotnet add package MRX.Json.Reflection
```

Part of the [MRX SDK](https://github.com/Mattia-Re/MRX-SDK).
